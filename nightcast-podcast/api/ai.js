export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});const key=process.env.GEMINI_API_KEY;if(!key)return res.status(503).json({error:'GEMINI_API_KEY is not configured.'});try{const body=typeof req.body==='string'?JSON.parse(req.body):req.body||{};const prompt=String(body.prompt||'').slice(0,90000);const model=process.env.GEMINI_MODEL||'gemini-3.7-flash';const system='You are Nightcast Podcast Director. Create natural multi-speaker dialogue from the supplied brief. Characters must sound distinct and react to each other. Never repeat the prompt, source text, instructions, or another speaker verbatim. Every turn must add a new idea, reaction, detail, disagreement, emotional beat, question, or revelation. Preserve supplied character details. Return only JSON.';const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(model)}:generateContent`,{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':key},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt}]}],systemInstruction:{parts:[{text:system}]},generationConfig:{temperature:.9,maxOutputTokens:7000,responseMimeType:'application/json',responseSchema:{type:'object',properties:{title:{type:'string'},lines:{type:'array',items:{type:'string'}}},required:['title','lines']}}})});const j=await r.json();if(!r.ok)return res.status(r.status).json({error:j?.error?.message||'Gemini request failed'});const text=j?.candidates?.[0]?.content?.parts?.map(p=>p.text||'').join('')||'';if(!text)return res.status(502).json({error:'AI returned no text'});return res.status(200).json(JSON.parse(text))}catch(e){return res.status(500).json({error:e?.message||'AI request failed'})}}