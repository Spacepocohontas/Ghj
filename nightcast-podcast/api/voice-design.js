export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{const b=req.body||{},key=process.env.ELEVENLABS_API_KEY||b.apiKey;if(!key)return res.status(400).json({error:'No ElevenLabs API key configured.'});const desc=String(b.description||'').trim();if(desc.length<20)return res.status(400).json({error:'Describe the voice in at least 20 characters.'});const r=await fetch('https://api.elevenlabs.io/v1/text-to-voice/design',{method:'POST',headers:{'Content-Type':'application/json','xi-api-key':key},body:JSON.stringify({voice_description:desc,model_id:b.modelId||'eleven_multilingual_ttv_v2',text:b.text||undefined,auto_generate_text:b.autoGenerateText!==false,should_enhance:b.enhance!==false})});const t=await r.text();let j;try{j=JSON.parse(t)}catch{j={error:t}}if(!r.ok)return res.status(r.status).json({error:j?.detail?.message||j?.detail||j?.error||'Voice design failed'});return res.status(200).json(j)}catch(e){return res.status(500).json({error:e.message||'Voice design failed'})}}