export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{const b=req.body||{},key=process.env.ELEVENLABS_API_KEY||b.apiKey;if(!key)return res.status(400).json({error:'No ElevenLabs API key configured.'});if(!b.generatedVoiceId||!b.name||!b.description)return res.status(400).json({error:'generatedVoiceId, name and description are required.'});const r=await fetch('https://api.elevenlabs.io/v1/text-to-voice',{method:'POST',headers:{'Content-Type':'application/json','xi-api-key':key},body:JSON.stringify({voice_name:String(b.name).slice(0,100),voice_description:String(b.description).slice(0,1000),generated_voice_id:String(b.generatedVoiceId)})});const t=await r.text();let j;try{j=JSON.parse(t)}catch{j={error:t}}if(!r.ok)return res.status(r.status).json({error:j?.detail?.message||j?.detail||j?.error||'Voice creation failed'});return res.status(200).json(j)}catch(e){return res.status(500).json({error:e.message||'Voice creation failed'})}}