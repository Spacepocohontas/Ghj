const K='nightcast-v1';let db=JSON.parse(localStorage.getItem(K)||'null')||{characters:[{id:'a',name:'Host',bio:'Curious interviewer',personality:'observant, quick-witted',style:'natural and conversational',catchphrase:'Let’s get into it.',memory:true},{id:'b',name:'Raven',bio:'Dry, mysterious co-host',personality:'sarcastic, intelligent, guarded',style:'slow, dry, low-key',catchphrase:'Obviously.',memory:true}],episodes:[]};let cast=['a','b'],current=null,utterTimer=null;const $=id=>document.getElementById(id);function persist(){localStorage.setItem(K,JSON.stringify(db));renderChars();renderEpisodes();renderCast()}function esc(s=''){return s.replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function renderCast(){$('cast').innerHTML=cast.map((id,i)=>{let c=db.characters.find(x=>x.id===id);return c?`<div class="castItem"><span><b>${esc(c.name)}</b><br><small>${esc(c.style)}</small></span><button onclick="removeCast('${id}')">×</button></div>`:''}).join('')||'<p>No cast yet.</p>'}window.removeCast=id=>{cast=cast.filter(x=>x!==id);renderCast()};$('addCast').onclick=()=>{let unused=db.characters.find(c=>!cast.includes(c.id));if(unused){cast.push(unused.id);renderCast()}};function renderChars(){$('characterList').innerHTML=db.characters.map(c=>`<article class="card"><h3>${esc(c.name)}</h3><div class="meta">Memory: ${c.memory?'ON':'OFF'}</div><p>${esc(c.bio||'No bio')}<br><br><b>Personality:</b> ${esc(c.personality||'')}<br><b>Style:</b> ${esc(c.style||'')}</p><div class="cardBtns"><button onclick="editChar('${c.id}')">Edit</button><button onclick="dupChar('${c.id}')">Duplicate</button><button onclick="delChar('${c.id}')">Delete</button></div></article>`).join('')||'<p>No characters.</p>'}window.editChar=id=>{let c=db.characters.find(x=>x.id===id);if(!c)return;$('charId').value=c.id;$('charName').value=c.name;$('charBio').value=c.bio;$('charPersonality').value=c.personality;$('charStyle').value=c.style;$('charCatch').value=c.catchphrase;$('charMemory').value=c.memory?'on':'off';$('charDialog').showModal()};window.dupChar=id=>{let c=db.characters.find(x=>x.id===id);db.characters.push({...c,id:crypto.randomUUID(),name:c.name+' Copy'});persist()};window.delChar=id=>{if(confirm('Delete this character?')){db.characters=db.characters.filter(c=>c.id!==id);cast=cast.filter(x=>x!==id);persist()}};$('newChar').onclick=()=>{$('charForm').reset();$('charId').value='';$('charDialog').showModal()};$('charForm').onsubmit=e=>{e.preventDefault();let id=$('charId').value||crypto.randomUUID(),c={id,name:$('charName').value,bio:$('charBio').value,personality:$('charPersonality').value,style:$('charStyle').value,catchphrase:$('charCatch').value,memory:$('charMemory').value==='on'};let n=db.characters.findIndex(x=>x.id===id);if(n<0)db.characters.push(c);else db.characters[n]=c;$('charDialog').close();persist()};function buildScript(){let topic=$('topic').value.trim()||'the strange things that happen after midnight',chars=cast.map(id=>db.characters.find(c=>c.id===id)).filter(Boolean);if(chars.length<2)chars=db.characters.slice(0,2);let rounds=$('length').value==='10'?12:$('length').value==='5'?7:4,lines=[`INTRO — Welcome to Nightcast. Tonight: ${topic}.`];for(let r=0;r<rounds;r++)chars.forEach((c,i)=>{let prompts=[`Let’s talk about ${topic}. What’s your first reaction?`,`Be honest: what does everyone misunderstand about this?`,`That sounds convenient. What are you leaving out?`,`What would you do if the situation got worse?`,`Give me one thing you actually believe about it.`];lines.push(`${c.name}: ${prompts[r%prompts.length]} ${c.catchphrase||''}`)});lines.push(`OUTRO — That’s Nightcast. Until next time.`);return lines}function generate(){let lines=buildScript();current={id:crypto.randomUUID(),title:$('title').value||'Untitled Nightcast',topic:$('topic').value,format:$('format').value,tone:$('tone').value,created:new Date().toISOString(),lines};$('transcript').textContent=lines.join('\n\n');$('player').classList.remove('hidden');$('speaker').textContent='Generated';$('line').textContent=current.title}$('generate').onclick=generate;function speakLines(i=0){if(!current||i>=current.lines.length)return;let text=current.lines[i],m=text.match(/^([^—:]+)[—:]/),speaker=m?m[1]:'Nightcast';$('speaker').textContent=speaker;$('line').textContent=text.replace(/^([^—:]+)[—:]/,'').trim();let u=new SpeechSynthesisUtterance(text.replace(/^([^—:]+)[—:]/,''));u.rate=.95;u.onend=()=>speakLines(i+1);speechSynthesis.speak(u)}$('play').onclick=()=>{speechSynthesis.cancel();speakLines()};$('stop').onclick=()=>speechSynthesis.cancel();$('save').onclick=()=>{if(!current)return;db.episodes.unshift(current);db.episodes=db.episodes.slice(0,100);persist()};$('download').onclick=()=>{if(!current)return;let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([current.lines.join('\n\n')],{type:'text/plain'}));a.download=(current.title||'nightcast').replace(/[^a-z0-9]+/gi,'-')+'.txt';a.click()};function renderEpisodes(){$('episodeList').innerHTML=db.episodes.map((e,i)=>`<article class="card"><h3>${esc(e.title)}</h3><div class="meta">${new Date(e.created).toLocaleString()} · ${esc(e.format)} · ${esc(e.tone)}</div><p>${esc(e.topic||'')}</p><button onclick="loadEp(${i})">Open</button> <button onclick="delEp(${i})">Delete</button></article>`).join('')||'<p>No saved episodes yet.</p>'}window.loadEp=i=>{current=db.episodes[i];$('title').value=current.title;$('topic').value=current.topic||'';$('format').value=current.format;$('tone').value=current.tone;$('transcript').textContent=current.lines.join('\n\n');$('player').classList.remove('hidden');document.querySelector('[data-tab="studio"]').click()};window.delEp=i=>{db.episodes.splice(i,1);persist()};$('exportAll').onclick=()=>{let a=document.createElement('a');a.href=URL.createObjectURL(new Blob([JSON.stringify(db,null,2)],{type:'application/json'}));a.download='nightcast-library.json';a.click()};$('reset').onclick=()=>{if(confirm('Delete all local Nightcast data?')){localStorage.removeItem(K);location.reload()}};document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>{document.querySelectorAll('.tab,.panel').forEach(x=>x.classList.remove('active'));b.classList.add('active');$(b.dataset.tab).classList.add('active')});$('newBtn').onclick=()=>document.querySelector('[data-tab="studio"]').click();renderCast();renderChars();renderEpisodes();